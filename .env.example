# Optional: Twitter/X.com bearer token (for API-based tweet search)
# Get one at https://developer.twitter.com/
# Note: Free tier does NOT include search. Basic tier ($200/mo) required.
# Without this, the app tries guest token search (may not work).
TWITTER_BEARER_TOKEN=

# Optional: Reddit API credentials (not needed — app uses public JSON API)
REDDIT_CLIENT_ID=
REDDIT_CLIENT_SECRET=
REDDIT_USER_AGENT=SentimentAnalyzer/2.0
